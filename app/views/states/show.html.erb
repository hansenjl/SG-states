<h1><%=@state.name%></h1>

Average Rating: <%= @state.visits.avg %>

<ul>
  <% @state.visits.highest_rated.each do |visit| %>
  <li><%= visit.user.username %> gave it a rating of <%= visit.rating%>/10</li>
  <%= link_to "Unnested Route", visit_path(visit)%>
  <% end %>
</ul>